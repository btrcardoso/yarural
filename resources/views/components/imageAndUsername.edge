<div class="flex flex-row">

    <div>
        <!-- Image -->
        <img class="w-10 rounded-full rounded-full shadow-2xl h-9 w-9" src="{{path}}" />
    </div>

    <div class="p-1">
        <!-- Username -->
        <label class="label">
            <span class="label-text"> 
                <a class="hover:underline" href="{{ route('user.show', {username: username}) }}">{{ username }}</a>     
            </span>
        </label>
    </div>

</div>